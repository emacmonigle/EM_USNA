test <- subset(readCounties, readCounties$STATE_NAME == s)
View(test)
match(c, test$COUNTY_NAME)
f <- 45
match(f, test$FID)
View(readCounties)
#read in county and state names
readCounties <- read_xls("species_data/US_county_names.xls")
View(readCounties)
readCounties <- subset(readCounties, readCounties$STATE_NAME == s)
match(c, readCounties$COUNTY_NAME)
readCounties$FID[58]
View(abundanceMatrix)
f <-  readCounties$FID[r]
r <- match(c, readCounties$COUNTY_NAME)
f <-  readCounties$FID[r]
abundanceMatrix[f, match(n, speciesCode)] = 1
View(I_all)
which(abundanceMatrix$OID == f)
r2 <- which(abundanceMatrix == f)
r2 <- which(abundanceMatrix$OID == f)
abundanceMatrix[r2 ,match(n, speciesCode)] = 1
#reset readCounties for next entry
readCounties <- read_xls("species_data/US_county_names.xls")
View(readCounties)
rm(test)
n <- "ILAM"
s <- "Alabama"
c <- "Baldwin"
#subset readCounties by state so that we can find unique county
readCounties <- subset(readCounties, readCounties$STATE_NAME == s)
View(readCounties)
r <- match(c, readCounties$COUNTY_NAME)
f <-  readCounties$FID[r]
r2 <- which(abundanceMatrix$OID == f)
abundanceMatrix[r2 ,match(n, speciesCode)] = 1
#reset readCounties for next entry
readCounties <- read_xls("species_data/US_county_names.xls")
n <- "ILAM"
s <- "Alabama"
c <- "Bibb"
#subset readCounties by state so that we can find unique county
readCounties <- subset(readCounties, readCounties$STATE_NAME == s)
r <- match(c, readCounties$COUNTY_NAME)
f <-  readCounties$FID[r]
r2 <- which(abundanceMatrix$OID == f)
abundanceMatrix[r2 ,match(n, speciesCode)] = 1
#reset readCounties for next entry
readCounties <- read_xls("species_data/US_county_names.xls")
View(I_all)
n <- "ILVE"
s <- "Louisiana"
c <- "East Feliciana"
#subset readCounties by state so that we can find unique county
readCounties <- subset(readCounties, readCounties$STATE_NAME == s)
r <- match(c, readCounties$COUNTY_NAME)
f <-  readCounties$FID[r]
r2 <- which(abundanceMatrix$OID == f)
abundanceMatrix[r2 ,match(n, speciesCode)] = 1
#reset readCounties for next entry
readCounties <- read_xls("species_data/US_county_names.xls")
View(countyNames)
#create a data frame with one row for each state and county
#create empty data frame to summarize presance in state by species
abundanceMatrix <- data.frame(matrix(rep(0, 18*3142),  ncol=18, nrow=3142))
#passing column names as species and row names as states
colnames(abundanceMatrix) <- c(speciesCode)
abundanceMatrix <- cbind(abundanceMatrix, countyNames)
#I_all
for (i in 1:nrow(I_all)){
n <- I_all$Symbol[i]
s <- I_all$State[i]
c <- I_all$County[i]
#subset readCounties by state so that we can find unique county
readCounties <- subset(readCounties, readCounties$STATE_NAME == s)
r <- match(c, readCounties$COUNTY_NAME)
f <-  readCounties$FID[r]
r2 <- which(abundanceMatrix$OID == f)
abundanceMatrix[r2 ,match(n, speciesCode)] = 1
#reset readCounties for next entry
readCounties <- read_xls("species_data/US_county_names.xls")
}
)
abundanceMatrix$sum <- rowSums(abundanceMatrix, 1:18)
abundanceMatrix$sum <- rowSums(abundanceMatrix[,1:18])
#save matrix with species presence data by state into csv
write.csv(abundanceMatrix, "Ilex_species_abundance_county.csv")
#load packages
library(dplyr)
library(readxl)
#~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
#read in data
species <- c("I_ambigua",
"I_amelanchier",
"I_cassine",
"I_collina",
"I_coriacea",
"I_cuthbertii",
"I_decidua",
"I_glabra",
"I_krugiana",
"I_laevigata",
"I_longipes",
"I_montana",
"I_mucronata",
"I_myrtifolia",
"I_opaca",
"I_opacaArenicola",
"I_verticillata",
"I_vomitoria")
#create for loop to read in all csv files
for (i in 1:length(species)) {
filename <- paste0(species)
wd <- paste0("species_data/", filename[i], "_DistributionData.csv")
assign(filename[i], read.csv(wd))
}
##Test inputs
n <- "ILVE"
n
n
#merge all files into one big file
I_all <- rbind(I_ambigua,
I_amelanchier,
I_cassine,
I_collina,
I_coriacea,
I_cuthbertii,
I_decidua,
I_glabra,
I_krugiana,
I_laevigata,
I_longipes,
I_montana,
I_mucronata,
I_myrtifolia,
I_opaca,
I_opacaArenicola,
I_verticillata,
I_vomitoria
)
#read in county and state names
readCounties <- read_xls("species_data/US_county_names.xls")
countyNames <- cbind(readCounties$FID, readCounties$STATE_NAME, readCounties$COUNTY_NAME)
colnames(countyNames) <- c("OID", "State", "County")
#create list with all species name in code
speciesCode <- unique(I_all$Symbol)
#remove observations without county level obs
dim(I_all) #4428 obs
I_all <- subset(I_all, I_all$County.FIP != "na")
dim(I_all) #4193 obs, removed 235 state only observations
#create a data frame with one row for each state and county
#create empty data frame to summarize presance in state by species
abundanceMatrix <- data.frame(matrix(rep(0, 18*3142),  ncol=18, nrow=3142))
library(dismo)
library(raster)
library(usethis)
library(sp)
library(devtools)
library(devtools)
library(rgdal)
library(rgeos)
library(rhwsd)
library(plyr)
library(ecospat)
library(ENMTools)
#Reading coordinates from where our genotypes were collected:
host.lines <- read.csv("data/Persea.csv", header=TRUE)
str(host.lines) #535 records
#Determining geographical extent of our data
max.lat <- ceiling(max(host.lines$Latitude))
min.lat <- floor(min(host.lines$Latitude))
max.lon <- ceiling(max(host.lines$Longitude))
min.lon <- floor(min(host.lines$Longitude))
geographic.extent.host.lines <- extent(x = c(min.lon, max.lon, min.lat, max.lat))
#removing duplicated records
#determine duplicated rows
dups <- duplicated(host.lines[, c('Latitude', 'Longitude')])
#load packages
library(dplyr)
library(readxl)
#~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
#read in data
species <- c("I_ambigua",
"I_amelanchier",
"I_cassine",
"I_collina",
"I_coriacea",
"I_cuthbertii",
"I_decidua",
"I_glabra",
"I_krugiana",
"I_laevigata",
"I_longipes",
"I_montana",
"I_mucronata",
"I_myrtifolia",
"I_opaca",
"I_opacaArenicola",
"I_verticillata",
"I_vomitoria")
#load packages
library(dplyr)
library(readxl)
#~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
#read in data
species <- c("I_ambigua",
"I_amelanchier",
"I_cassine",
"I_collina",
"I_coriacea",
"I_cuthbertii",
"I_decidua",
"I_glabra",
"I_krugiana",
"I_laevigata",
"I_longipes",
"I_montana",
"I_mucronata",
"I_myrtifolia",
"I_opaca",
"I_opacaArenicola",
"I_verticillata",
"I_vomitoria")
#create for loop to read in all csv files
for (i in 1:length(species)) {
filename <- paste0(species)
wd <- paste0("species_data/", filename[i], "_DistributionData.csv")
assign(filename[i], read.csv(wd))
}
#merge all files into one big file
I_all <- rbind(I_ambigua,
I_amelanchier,
I_cassine,
I_collina,
I_coriacea,
I_cuthbertii,
I_decidua,
I_glabra,
I_krugiana,
I_laevigata,
I_longipes,
I_montana,
I_mucronata,
I_myrtifolia,
I_opaca,
I_opacaArenicola,
I_verticillata,
I_vomitoria
)
#save combined species data into one csv
write.csv(I_all, "I_all.csv")
#read in county and state names
readCounties <- read_xls("species_data/US_county_names.xls")
countyNames <- cbind(readCounties$FID, readCounties$STATE_NAME, readCounties$COUNTY_NAME)
colnames(countyNames) <- c("OID", "State", "County")
#create list with all species name in code
speciesCode <- unique(I_all$Symbol)
#remove observations without county level obs
dim(I_all) #4428 obs
I_all <- subset(I_all, I_all$County.FIP != "na")
dim(I_all) #4193 obs, removed 235 state only observations
#create a data frame with one row for each state and county
#create empty data frame to summarize presance in state by species
abundanceMatrix <- data.frame(matrix(rep(0, 18*3142),  ncol=18, nrow=3142))
#passing column names as species and row names as states
colnames(abundanceMatrix) <- c(speciesCode)
abundanceMatrix <- cbind(abundanceMatrix, countyNames)
##Test inputs
n <- "ILVE"
n
View(readCounties)
View(abundanceMatrix)
#WARNING: will run for about 5 minutes, theres a lot of steps to process
#I_all
for (i in 1:nrow(I_all)){
n <- I_all$Symbol[i]
s <- I_all$State[i]
c <- I_all$County[i]
#subset readCounties by state so that we can find unique county
readCounties <- subset(readCounties, readCounties$STATE_NAME == s)
r <- match(c, readCounties$COUNTY_NAME)
f <-  readCounties$FID[r]
r2 <- which(abundanceMatrix$OID == f)
abundanceMatrix[r2 ,match(n, speciesCode)] = 1
#reset readCounties for next entry
readCounties <- read_xls("species_data/US_county_names.xls")
}
View(abundanceMatrix)
#load packages
library(dplyr)
library(readxl)
#~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
#read in data
species <- c("I_ambigua",
"I_amelanchier",
"I_cassine",
"I_collina",
"I_coriacea",
"I_cuthbertii",
"I_decidua",
"I_glabra",
"I_krugiana",
"I_laevigata",
"I_longipes",
"I_montana",
"I_mucronata",
"I_myrtifolia",
"I_opaca",
"I_opacaArenicola",
"I_verticillata",
"I_vomitoria")
#create for loop to read in all csv files
for (i in 1:length(species)) {
filename <- paste0(species)
wd <- paste0("species_data/", filename[i], "_DistributionData.csv")
assign(filename[i], read.csv(wd))
}
#merge all files into one big file
I_all <- rbind(I_ambigua,
I_amelanchier,
I_cassine,
I_collina,
I_coriacea,
I_cuthbertii,
I_decidua,
I_glabra,
I_krugiana,
I_laevigata,
I_longipes,
I_montana,
I_mucronata,
I_myrtifolia,
I_opaca,
I_opacaArenicola,
I_verticillata,
I_vomitoria
)
View(I_amelanchier)
View(I_all)
#save combined species data into one csv
write.csv(I_all, "I_all.csv")
#read in county and state names
readCounties <- read_xls("species_data/US_county_names.xls")
View(readCounties)
countyNames <- cbind(readCounties$FID, readCounties$STATE_NAME, readCounties$COUNTY_NAME)
colnames(countyNames) <- c("OID", "State", "County")
View(countyNames)
#create list with all species name in code
speciesCode <- unique(I_all$Symbol)
#remove observations without county level obs
dim(I_all) #4428 obs
I_all <- subset(I_all, I_all$County.FIP != "na")
dim(I_all) #4193 obs, removed 235 state only observations
#create a data frame with one row for each state and county
#create empty data frame to summarize presance in state by species
abundanceMatrix <- data.frame(matrix(rep(0, 18*3142),  ncol=18, nrow=3142))
View(abundanceMatrix)
#passing column names as species and row names as states
colnames(abundanceMatrix) <- c(speciesCode)
abundanceMatrix <- cbind(abundanceMatrix, countyNames)
#load packages
library(dplyr)
library(readxl)
#~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
#read in data
species <- c("I_ambigua",
"I_amelanchier",
"I_cassine",
"I_collina",
"I_coriacea",
"I_cuthbertii",
"I_decidua",
"I_glabra",
"I_krugiana",
"I_laevigata",
"I_longipes",
"I_montana",
"I_mucronata",
"I_myrtifolia",
"I_opaca",
"I_opacaArenicola",
"I_verticillata",
"I_vomitoria")
#create for loop to read in all csv files
for (i in 1:length(species)) {
filename <- paste0(species)
wd <- paste0("species_data/", filename[i], "_DistributionData.csv")
assign(filename[i], read.csv(wd))
}
View(I_coriacea)
View(I_coriacea)
#merge all files into one big file
I_all <- rbind(I_ambigua,
I_amelanchier,
I_cassine,
I_collina,
I_coriacea,
I_cuthbertii,
I_decidua,
I_glabra,
I_krugiana,
I_laevigata,
I_longipes,
I_montana,
I_mucronata,
I_myrtifolia,
I_opaca,
I_opacaArenicola,
I_verticillata,
I_vomitoria
)
View(I_all)
#save combined species data into one csv
write.csv(I_all, "I_all.csv")
#read in county and state names
readCounties <- read_xls("species_data/US_county_names.xls")
#load packages
library(dplyr)
library(readxl)
#~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
#read in data
species <- c("I_ambigua",
"I_amelanchier",
"I_cassine",
"I_collina",
"I_coriacea",
"I_cuthbertii",
"I_decidua",
"I_glabra",
"I_krugiana",
"I_laevigata",
"I_longipes",
"I_montana",
"I_mucronata",
"I_myrtifolia",
"I_opaca",
"I_opacaArenicola",
"I_verticillata",
"I_vomitoria")
#create for loop to read in all csv files
for (i in 1:length(species)) {
filename <- paste0(species)
wd <- paste0("species_data/", filename[i], "_DistributionData.csv")
assign(filename[i], read.csv(wd))
}
#merge all files into one big file
I_all <- rbind(I_ambigua,
I_amelanchier,
I_cassine,
I_collina,
I_coriacea,
I_cuthbertii,
I_decidua,
I_glabra,
I_krugiana,
I_laevigata,
I_longipes,
I_montana,
I_mucronata,
I_myrtifolia,
I_opaca,
I_opacaArenicola,
I_verticillata,
I_vomitoria
)
View(I_all)
#read in county and state names
readCounties <- read_xls("species_data/US_county_names.xls")
#save combined species data into one csv
write.csv(I_all, "species_data/I_all.csv")
#read in county and state names
readCounties <- read_xls("species_data/US_county_names.xls")
countyNames <- cbind(readCounties$FID, readCounties$STATE_NAME, readCounties$COUNTY_NAME)
colnames(countyNames) <- c("OID", "State", "County")
#create list with all species name in code
speciesCode <- unique(I_all$Symbol)
#remove observations without county level obs
dim(I_all) #4428 obs
I_all <- subset(I_all, I_all$County.FIP != "na")
#create a data frame with one row for each state and county
#create empty data frame to summarize presance in state by species
abundanceMatrix <- data.frame(matrix(rep(0, 18*3142),  ncol=18, nrow=3142))
#passing column names as species and row names as states
colnames(abundanceMatrix) <- c(speciesCode)
abundanceMatrix <- cbind(abundanceMatrix, countyNames)
##Test inputs
n <- "ILVE"
n
#WARNING: will run for about 5 minutes, theres a lot of steps to process
#I_all
for (i in 1:nrow(I_all)){
n <- I_all$Symbol[i]
s <- I_all$State[i]
c <- I_all$County[i]
#subset readCounties by state so that we can find unique county
readCounties <- subset(readCounties, readCounties$STATE_NAME == s)
r <- match(c, readCounties$COUNTY_NAME)
f <-  readCounties$FID[r]
r2 <- which(abundanceMatrix$OID == f)
abundanceMatrix[r2 ,match(n, speciesCode)] = 1
#reset readCounties for next entry
readCounties <- read_xls("species_data/US_county_names.xls")
}
View(abundanceMatrix)
#create a summary column of number of species by county
abundanceMatrix$sum <- rowSums(abundanceMatrix[,1:18])
#save matrix with species presence data by state into csv
write.csv(abundanceMatrix, "Ilex_species_abundance_county.csv")
#save matrix with species presence data by state into csv
write.csv(abundanceMatrix, "species_data\Ilex_species_abundance_county.csv")
#save matrix with species presence data by state into csv
write.csv(abundanceMatrix, "species_data/Ilex_species_abundance_county.csv")
